```
🫧 클래스와 객체
객체(object)는 사용할 수 있는 실체를 의미하며, 클래스는 객체를 만들기 위한 설계도와 같음.

클래스는 왜 사용할까??
변수 : 다양한 혀애의 데이터를 저장하기 위해 각각의 데이터를 저장할 수 있는 변수라는 문법 요소를 만들어 사용

배열 : 데이터의 종류가 많아질수록 데이터의 개수만큼 변수명을 짓거나 관리하는 일이 버거워짐. 
       이런 문제를 해결해기 위해 만든 문법요소가 '배열'

구조체 : 배열은 같은 자료형만 묶을 수 있으므로 학생들의 성적(int)과 반 평균(double)은 1개의 배열로 관리할 수 없음. 
        이를 보완하기 위해 만든 문법요소가 '구조체(struct)'다. 

클래스 : 반 평균 성적을 출력하거나 반 학생들의 총점을 계산하는 등과 같은 기능을 
        추가하면 반의 성적과 관련된 모든 내용을 효율적으로 처리할 수 있다. 
        이것이 바로 '클래스(class)'다. 다양한 자료형의 데이터를 묶어 관리할 수 있을 뿐 아니라 
        데이터를 처리하는 다양한 기능까지 함께 관리하는 문법

⭐ 클래스에 포함돼 클래스 안에 있는 데이터를 처리하는 기능을 '메서드'라고 한다.

🫧 절차지향과 객체지향 이해하기
절차지향형 프로그래밍(PP : procedural programming)은 순서에 맞춰 단계적으로 실행하도록 명령어를 나열하는 방식을 말함. 
객체지향형 프로그래밍(OOP : Object Oriented programming) : 객체 단위로 수행하는 방식을 말함

자바에서 제공하는 객체지향 문법 요소
문법요소에는 '클래스'와 '인터페이스'가 있고, 클래스는 다시 '일반 클래스'와 '추상 클래스(abstract class)'로 나뉜다.

즉 클래스나 인터페이스를 활용해 객체를 직, 간접적으로 만들고 이를 활용해 프로그램을 실행한다는 의미

🫧 클래스 구조 살펴보기
클래스 밖에 올 수 있는 3가지 : 패키지, 임포트, 외부 클래스

외부 클래스(external class) : 클래스의 외부에 또 다른 클래스가 또 포함될 수 있다. 
즉 1개의 .java 파일에 여러 개의 클래스가 포함될 수 있다. 단 외부 클래스에는 public 키워들 붙일 수 없다.


클래스 안에 올수 있는 4가지
public class A {
	int a = 3;	// 필드
	double abc() {...}	// 메서드
	A() {...}	// 생성자
	class 클래스명 {...}	// 이너 클래스
}

필드(field) : 클래스의 특징(속성)을 나타내는 변수다. 
              만일 이 클래스가 한 사람에 관련된 정보를 담고 있다면 필드값으로 나이(int age = 20)등이 포함 될 수 있을 것.

메서드 (method) : 클래스가 지니고 있는 기능(함수)을 나타낸다. 
                  한 사람에 관련된 클래스라면 일하기(void working() {...}) 등이 포함 될 수 있다.

생성자 (constructor) : 생성자(A () {...})는 클래스의 객체를 생성하는 역할을 담당한다.

이너 클래스 (inner class) : 클래스의 내부에도 클래스가 포함될 수 있다.

내부에 올 수 있는 4가지 구성요소들 중 생성자를 제외한 3가지 요소를 '클래스의 멤버(member)'라고 함.

🫧 클래스와 객체 구분하기
클래스의 생성자로 객체를 만드는 과정을 '인스턴스화(instantiation)이라 하고,
인스턴스화로 만들어진 객체를 '인스턴스(instance)'라고 한다. 
객체 속에는 클래스의 내부 구성 요고 중 생성자를 제외한 나머지 요소가 포함 되어 있는데
이를 '인스턴스 멤버(instance member)'라고 한다. 

🫧 객체의 생성과 활용

클래스명 참조 변수명 = new 생성자();

포인트 연산자 사용하기 : '해당 위치에  객체 안을 보라'

참조 변수명.필드명
참조 변수명.메서드명()

A a = new A();
System.out.print(a.m);	// 필드 활용
a.print();	// 매서드 활용

```
